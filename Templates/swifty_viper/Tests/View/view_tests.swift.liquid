//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by lcr on {{ date }}.
//

@testable import {{ module_info.project_name }}
import XCTest

class {{ module_info.name }}ViewTests: XCTestCase {
    var viewController: {{ module_info.name }}Controller!
    var presenter: Mock{{ module_info.name }}Presenter!

    override func setUp() {
        super.setUp()
        presenter = Mock{{ module_info.name }}Presenter()
        viewController = {{ module_info.name }}ViewController.instantiate()
        viewController.presenter = presenter
        if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            windowScene.windows.first?.rootViewController = viewController
            windowScene.windows.first?.makeKeyAndVisible()
        }
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }

}
