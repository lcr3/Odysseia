//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by lcr on {{ date }}.
//

@testable import {{ module_info.project_name }}
import XCTest

class {{ module_info.name }}PresenterTest: XCTestCase {
    var view: Mock{{ module_info.name }}View!
    var router: Mock{{ module_info.name }}Router!
    var interactor: Mock{{ module_info.name }}Interactor!
    var presenter: {{ module_info.name }}Presenter!

    override func setUp() {
        super.setUp()
        view = Mock{{ module_info.name }}View()
        router = Mock{{ module_info.name }}Router()
        interactor = Mock{{ module_info.name }}Interactor()
        presenter = SettingPresenter(view: view, router: router, interactor, interactor)
    }

    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }
}
